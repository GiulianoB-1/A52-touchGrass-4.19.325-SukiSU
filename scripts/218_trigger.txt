Phase 219 focused qseecomd/ION failure-boundary RS48 recorder

Hardware evidence from A52_RAW_RAMOOPS_20260803_205941.zip:
- 1,028 valid R48/CRC32C records were recovered, sequences 1752 through 2783, with four sequence gaps
- the frozen early 1 MiB snapshot and the later raw export are byte-identical
- 880 of 892 Phase 218 KEYPOST records were generated by sscrpcd opening /dev/ion 442 times
- this broad open tracing overwrote the earlier qseecomd startup boundary
- surviving Keymaster QSEE perf-disable, perf-enable and send-command ioctls all return 0
- /vendor/bin/qseecomd still exits with status 255 about 23 ms after exec and repeats about every five seconds
- the kernel remains alive beyond 84 seconds

Phase 219 is observation-only:
- disables the broad Phase 218 secure-path open recorder
- records secure opens only for qseecomd, keymaster, gatekeeper, keystore, android.hardware and vendor.samsung tasks
- records task-filtered ION ioctl metadata and exact return values for those tasks
- retains QSEECom app, listener, firmware-fragment and ioctl metadata from Phase 218

The corrected payload is generated against the exact post-Phase-217 source hashes used by the combined wrapper. Its nested Phase-217-plus-Phase-219 self-test and exact output comparison pass locally.

It records only paths, ioctl numbers, return values, sizes, directions and allocation metadata. No command buffers, response buffers, key material, authentication tokens, Binder payloads or process memory are recorded.

No ION, QSEECom, SCM, KeyMint, DTB, DTBO, ramdisk, clock, regulator or power behavior is changed. Existing three-copy R48 transport, 48 Reed-Solomon parity symbols, CRC32C, decoder tests and boot-image invariant audits remain mandatory.
