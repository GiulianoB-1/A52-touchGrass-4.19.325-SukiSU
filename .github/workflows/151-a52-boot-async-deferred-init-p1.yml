name: 151 - A52 Async Probe + Deferred Page Init P1

on:
  push:
    branches:
      - agent/a52-p151-boot-async-deferred-init
    paths:
      - .github/workflows/151-a52-boot-async-deferred-init-p1.yml
      - scripts/151_apply_boot_async_deferred_init_p1.py
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: a52-boot-async-deferred-p151-${{ github.ref }}
  cancel-in-progress: true

env:
  JOBS: '4'
  CCACHE_DIR: ${{ github.workspace }}/.ccache
  CCACHE_MAXSIZE: 5G
  IMAGE: artifacts/Image-touchgrass-4.19.206-resukisu-v4.1.0-susfs-v1.4.2-manual-core
  ANYKERNEL_COMMIT: 1c9a500dd4aa8081952523126e97eb155aed941b
  P149_BASELINE_SHA: a8398165830dcdbefa6256b59fd14db0115e8399
  P149_BASELINE_RUN: '35983892365'

jobs:
  build:
    name: Build exact P149 + async probe + deferred page init
    runs-on: ubuntu-22.04
    timeout-minutes: 360

    steps:
      - name: Check out exact boot-tested P149 baseline
        uses: actions/checkout@v4
        with:
          ref: ${{ env.P149_BASELINE_SHA }}

      - name: Free runner disk space
        run: |
          sudo rm -rf /usr/local/lib/android /usr/share/dotnet /opt/ghc || true
          df -h

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            git curl xz-utils zip unzip make gcc g++ bc bison flex ccache patch \
            libssl-dev libelf-dev python3 rsync cpio lz4 binutils file tar

      - name: Restore compiler cache
        id: ccache_restore
        uses: actions/cache/restore@v4
        with:
          path: .ccache
          key: a52xq-p151-boot-async-deferred-${{ runner.os }}-${{ github.sha }}
          restore-keys: |
            a52xq-p149-ufs-runtime-autosuspend-${{ runner.os }}-
            a52xq-p148-qcom-battery-${{ runner.os }}-
            a52xq-p146-teo-active-${{ runner.os }}-
            a52xq-f2fs325-p1-${{ runner.os }}-

      - name: Configure compiler cache
        run: |
          mkdir -p "$CCACHE_DIR"
          ccache --max-size "$CCACHE_MAXSIZE"
          ccache --set-config=compression=true
          ccache --set-config=compression_level=6
          ccache --zero-stats

      - name: Restore exact prepared P134 feature source cache
        id: p134_source
        uses: actions/cache/restore@v4
        with:
          path: workspace/touchgrass-a52xq
          key: a52xq-base35633887441-p134-src-v1-${{ runner.os }}-${{ hashFiles('scripts/118_apply_f2fs_stable_325_p1.sh', 'scripts/119_apply_f2fs_gc_merge_p2.py', 'scripts/121_apply_f2fs_inmem_curseg_p3a.py', 'scripts/122_apply_f2fs_atgc_p3b.py', 'scripts/123_enable_f2fs_atgc_p3c.py', 'scripts/124_apply_f2fs_atgc_p4.py', 'scripts/125_apply_f2fs_block_age_p5a.py', 'scripts/126_enable_f2fs_block_age_p5b.py', 'scripts/127_apply_f2fs_gc_cp_p6.py', 'scripts/128_apply_f2fs_correctness_p7.py', 'scripts/129_apply_bpf_bootfix_p1.py', 'scripts/134_apply_bpf_devmap_rndis_clean_fix.py') }}

      - name: Require exact prepared source boundary
        run: |
          set -Eeuo pipefail
          echo "P134 cache hit: ${{ steps.p134_source.outputs.cache-hit }}"
          test "${{ steps.p134_source.outputs.cache-hit }}" = "true"
          test -d workspace/touchgrass-a52xq/.git

      - name: Prepare workspace
        run: |
          set -Eeuo pipefail
          mkdir -p artifacts release
          chmod +x scripts/*.sh scripts/*.py

      - name: Replay production chain through P148
        run: |
          set -Eeuo pipefail

          python3 scripts/135_remove_project_diagnostics.py workspace/touchgrass-a52xq artifacts/production-nodiag-p135
          python3 scripts/136_apply_a619_kgsl_p6_pm.py workspace/touchgrass-a52xq
          python3 scripts/137_apply_a52_boot_rescue.py workspace/touchgrass-a52xq
          python3 scripts/138_apply_a619_kgsl_p7_cp_init.py workspace/touchgrass-a52xq
          python3 scripts/139_apply_mglru_modern_p4_pagecache.py workspace/touchgrass-a52xq
          python3 scripts/140_apply_schedutil_iowait_android17_p1.py workspace/touchgrass-a52xq
          python3 scripts/141_apply_schedutil_android17_limits_sync_p2.py workspace/touchgrass-a52xq
          python3 scripts/142_apply_uclamp_android17_reinc_idle_p3.py workspace/touchgrass-a52xq
          python3 scripts/143_apply_uclamp_android17_rq_idle_p4.py workspace/touchgrass-a52xq

          python3 scripts/145_apply_teo_baseline_p1.py \
            workspace/touchgrass-a52xq backports/145_teo_4.19.c \
            | tee artifacts/cpuidle-teo-baseline-p145.txt
          python3 scripts/146_apply_teo_active_recentfix_p2.py workspace/touchgrass-a52xq \
            | tee artifacts/cpuidle-teo-active-p146.txt
          python3 scripts/148_apply_qcom_lpm_fixpack_p1.py workspace/touchgrass-a52xq \
            | tee artifacts/qcom-lpm-fixpack-p148.txt

      - name: Replay boot-tested P149 UFS runtime PM/autosuspend
        run: |
          set -Eeuo pipefail
          python3 scripts/149_apply_ufs_runtime_autosuspend_p1.py workspace/touchgrass-a52xq \
            | tee artifacts/ufs-runtime-autosuspend-p149.txt

          KERNEL=workspace/touchgrass-a52xq
          grep -Fq 'A52 UFS P149: precise devfreq + runtime-PM autosuspend' "$KERNEL/drivers/scsi/ufs/ufshcd.c"
          grep -Fq 'hba->caps |= UFSHCD_CAP_RPM_AUTOSUSPEND;' "$KERNEL/drivers/scsi/ufs/ufs-qcom.c"

      - name: Apply P151 selective async probing + deferred page init
        run: |
          set -Eeuo pipefail

          curl -fsSL \
            "https://raw.githubusercontent.com/${{ github.repository }}/${{ github.sha }}/scripts/151_apply_boot_async_deferred_init_p1.py" \
            -o /tmp/151_apply_boot_async_deferred_init_p1.py

          python3 -m py_compile /tmp/151_apply_boot_async_deferred_init_p1.py
          python3 /tmp/151_apply_boot_async_deferred_init_p1.py workspace/touchgrass-a52xq \
            | tee artifacts/boot-async-deferred-p151.txt

          KERNEL=workspace/touchgrass-a52xq
          grep -Fq 'CONFIG_DEFERRED_STRUCT_PAGE_INIT=y' "$KERNEL/arch/arm64/configs/a52xq_defconfig"
          grep -Fq 'PROBE_PREFER_ASYNCHRONOUS, /* A52 P151 */' "$KERNEL/drivers/nfc/sec_nfc.c"
          grep -Fq 'PROBE_PREFER_ASYNCHRONOUS, /* A52 P151 */' "$KERNEL/drivers/fingerprint/et7xx-spi.c"

          git -C "$KERNEL" diff --check
          git -C "$KERNEL" diff -- \
            arch/arm64/configs/a52xq_defconfig \
            drivers/nfc/sec_nfc.c \
            drivers/fingerprint/et7xx-spi.c \
            > artifacts/boot-async-deferred-p151.diff

      - name: Build integrated kernel
        run: |
          set -o pipefail
          echo '=== CCACHE BEFORE ==='
          ccache --show-stats || true
          START=$(date +%s)
          ./scripts/57_build_resukisu_susfs_manual_detection.sh \
            2>&1 | tee artifacts/boot-async-deferred-p151-build.log
          RC=${PIPESTATUS[0]}
          END=$(date +%s)
          echo "kernel_build_seconds=$((END-START))"
          echo '=== CCACHE AFTER ==='
          ccache --show-stats || true
          exit "$RC"

      - name: Audit P151 image and final config
        run: |
          set -Eeuo pipefail
          KERNEL=workspace/touchgrass-a52xq
          CFG="$KERNEL/out/.config"
          NFC="$KERNEL/drivers/nfc/sec_nfc.c"
          FP="$KERNEL/drivers/fingerprint/et7xx-spi.c"
          UFS="$KERNEL/drivers/scsi/ufs/ufshcd.c"

          test -s "$IMAGE"
          test -s "$KERNEL/out/vmlinux"
          test -s "$CFG"

          grep -Fxq 'CONFIG_DEFERRED_STRUCT_PAGE_INIT=y' "$CFG"
          grep -Fxq 'CONFIG_SPARSEMEM=y' "$CFG"
          grep -Fxq 'CONFIG_NO_BOOTMEM=y' "$CFG"

          test "$(grep -Fc 'PROBE_PREFER_ASYNCHRONOUS, /* A52 P151 */' "$NFC")" -eq 1
          test "$(grep -Fc 'PROBE_PREFER_ASYNCHRONOUS, /* A52 P151 */' "$FP")" -eq 1

          # Retain exact P149 and earlier production stack.
          grep -Fq 'A52 UFS P149: precise devfreq + runtime-PM autosuspend' "$UFS"
          grep -Fq 'A52 QCOM LPM P1: raw idle locks + short-idle tick + hotpath cleanup' "$KERNEL/drivers/cpuidle/lpm-levels.c"
          grep -Fq 'A52 P148 BATTERY: Qualcomm LPM + WALT/schedutil correctness' "$KERNEL/kernel/sched/cpufreq_schedutil.c"
          grep -Fq 'A52 UCLAMP P4: Android17 rq access helpers + idle-rq semantics' "$KERNEL/kernel/sched/sched.h"
          grep -Fq 'A52 BOOT RESCUE P137' "$KERNEL/drivers/input/input.c"
          grep -Fq 'static void dev_map_hash_remove_netdev' "$KERNEL/kernel/bpf/devmap.c"

          {
            echo 'P151 final config:'
            grep -E '^(CONFIG_(DEFERRED_STRUCT_PAGE_INIT|SPARSEMEM|NO_BOOTMEM)=)' "$CFG"
            echo
            echo 'Async drivers:'
            grep -n 'PROBE_PREFER_ASYNCHRONOUS, /\* A52 P151 \*/' "$NFC" "$FP"
          } | tee artifacts/boot-async-deferred-p151-audit.txt

          git -C "$KERNEL" diff --check

      - name: Check out pinned AnyKernel3
        uses: actions/checkout@v4
        with:
          repository: osm0sis/AnyKernel3
          ref: ${{ env.ANYKERNEL_COMMIT }}
          path: packaging/AnyKernel3
          fetch-depth: 1

      - name: Package P151 flashable ZIP
        run: |
          set -Eeuo pipefail
          cp scripts/09_package_a52xq_anykernel.sh scripts/.package-p151.sh
          sed -i "s/grep -Fxq '# CONFIG_KSU_SUSFS is not set'/grep -Fxq 'CONFIG_KSU_SUSFS=y'/" scripts/.package-p151.sh
          sed -i 's/SUSFS must remain disabled for this package/Kernel config does not enable SUSFS core/' scripts/.package-p151.sh

          python3 - <<'PY'
          from pathlib import Path
          p = Path('scripts/.package-p151.sh')
          s = p.read_text()
          anchor = 'dump_boot;\nwrite_boot;'
          if s.count(anchor) != 1:
              raise SystemExit(f'unexpected boot-write anchor count: {s.count(anchor)}')
          s = s.replace(
              anchor,
              'dump_boot;\n'
              'patch_cmdline "sched_eevdf" "sched_eevdf=1";\n'
              'patch_cmdline "sched_cass" "sched_cass=1";\n'
              'write_boot;',
              1,
          )
          p.write_text(s)
          PY

          python3 scripts/88_patch_anykernel_vulkan_renderengine.py scripts/.package-p151.sh
          sed -i 's/kernel.string=touchGrass A52xq Linux $TARGET_VERSION + ReSukiSU v4.1.0/kernel.string=touchGrass A52xq Linux $TARGET_VERSION + ReSukiSU v4.1.0 + P149-BASE35983892365 + BOOT-ASYNC-DEFERRED-P151/' scripts/.package-p151.sh
          sed -i 's/ReSukiSU-v4.1.0-BUILD-VERIFIED-FLASHABLE.zip/A52-P151-BOOT-ASYNC-DEFERRED-P1.zip/' scripts/.package-p151.sh
          chmod +x scripts/.package-p151.sh
          ./scripts/.package-p151.sh 4.19.206 "$IMAGE" packaging/AnyKernel3 release
          rm -f scripts/.package-p151.sh

      - name: Verify package
        run: |
          set -Eeuo pipefail
          ZIP="$(find release -maxdepth 1 -name '*.zip' -type f | head -1)"
          test -s "$ZIP"
          unzip -p "$ZIP" anykernel.sh | grep -Fq 'patch_cmdline "sched_eevdf" "sched_eevdf=1";'
          unzip -p "$ZIP" anykernel.sh | grep -Fq 'patch_cmdline "sched_cass" "sched_cass=1";'
          unzip -p "$ZIP" anykernel.sh | grep -Fq 'install_vulkan_re_module;'
          sha256sum "$ZIP" | tee artifacts/package-sha256.txt

      - name: Save compiler cache
        if: always() && steps.ccache_restore.outputs.cache-hit != 'true'
        uses: actions/cache/save@v4
        with:
          path: .ccache
          key: a52xq-p151-boot-async-deferred-${{ runner.os }}-${{ github.sha }}

      - name: Upload P151 flashable ZIP and audit
        uses: actions/upload-artifact@v4
        with:
          name: A52XQ-BASE35983892365-BOOT-ASYNC-DEFERRED-P151-${{ github.run_number }}
          path: |
            release/
            artifacts/boot-async-deferred-p151.txt
            artifacts/boot-async-deferred-p151.diff
            artifacts/boot-async-deferred-p151-audit.txt
            artifacts/package-sha256.txt
          if-no-files-found: error
          retention-days: 14

# Exact baseline:
# P149 run 35983892365 / a8398165830dcdbefa6256b59fd14db0115e8399
#
# P151 changes only:
# - CONFIG_DEFERRED_STRUCT_PAGE_INIT=y
# - Samsung sec_nfc driver prefers asynchronous probing
# - Samsung ET7xx fingerprint driver prefers asynchronous probing
#
# Deliberately left synchronous:
# UFS, display, PMIC/SPMI, clocks, regulators, interconnect, IOMMU/SMMU,
# CPUfreq/devfreq and eSE.
