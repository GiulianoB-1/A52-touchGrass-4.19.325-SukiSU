name: Phase 234 RSCC collector relay

on:
  workflow_run:
    workflows:
      - 87 - Fix Workflow 84 post-build audit
    types:
      - requested

permissions:
  contents: read

concurrency:
  group: phase234-rscc-relay
  cancel-in-progress: true

jobs:
  release-runner:
    name: Release runner for inherited Phase 234 build
    runs-on: ubuntu-22.04
    steps:
      - run: echo 'Phase 234 relay runner released; inherited build continues independently.'
