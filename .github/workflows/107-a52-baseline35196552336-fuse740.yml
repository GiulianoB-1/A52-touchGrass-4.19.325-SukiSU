name: 107 - A52 Baseline 35196552336 + Native FUSE 7.40

on:
  push:
    branches:
      - agent/a52-baseline-35196552336-fuse740-v1
    paths:
      - .github/workflows/107-a52-baseline35196552336-fuse740.yml
      - scripts/98_apply_fuse_passthrough_modern_p1.py
      - scripts/99_apply_fuse_passthrough_modern_p2.py
      - scripts/101_apply_fuse_passthrough_modern_p3.py
      - scripts/102_apply_fuse_negotiation_probe.py
      - scripts/103_apply_fuse_740_init_compat.py
      - scripts/104_apply_fuse_740_upstream_passthrough.py
      - scripts/104_apply_a619_gpu_undervolt_p1.py
      - scripts/105_apply_a619_gpu_undervolt_p1_diag.py
      - scripts/106_apply_mglru_modern_p1.py
      - scripts/57_build_resukisu_susfs_manual_detection.sh
      - scripts/09_package_a52xq_anykernel.sh
      - scripts/88_patch_anykernel_vulkan_renderengine.py
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: a52-base35196552336-fuse740-${{ github.ref }}
  cancel-in-progress: true

env:
  JOBS: '4'
  CCACHE_DIR: ${{ github.workspace }}/.ccache
  CCACHE_MAXSIZE: 5G
  IMAGE: artifacts/Image-touchgrass-4.19.206-resukisu-v4.1.0-susfs-v1.4.2-manual-core
  ANYKERNEL_COMMIT: 1c9a500dd4aa8081952523126e97eb155aed941b

jobs:
  build:
    name: Build baseline 35196552336 + native FUSE 7.40
    runs-on: ubuntu-22.04
    timeout-minutes: 240

    steps:
      - name: Check out project
        uses: actions/checkout@v4

      - name: Free runner disk space
        run: |
          sudo rm -rf /usr/local/lib/android /usr/share/dotnet /opt/ghc || true
          df -h

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            git curl xz-utils zip unzip make gcc g++ bc bison flex ccache patch \
            libssl-dev libelf-dev python3 rsync cpio lz4 binutils file tar

      - name: Restore compiler cache
        id: ccache_restore
        uses: actions/cache/restore@v4
        with:
          path: .ccache
          key: a52xq-base35196552336-fuse740-${{ runner.os }}-v1
          restore-keys: |
            a52xq-fuse-740-passthrough-btp2c-${{ runner.os }}-
            a52xq-fuse-neg-probe-btp2c-${{ runner.os }}-
            a52xq-fuse-modern-p3-btp2c-${{ runner.os }}-
            a52xq-bt-p2c-rxpm-${{ runner.os }}-
            a52xq-gpu-p5-bbr3-fq-${{ runner.os }}-

      - name: Configure compiler cache
        run: |
          mkdir -p "$CCACHE_DIR"
          ccache --max-size "$CCACHE_MAXSIZE"
          ccache --set-config=compression=true
          ccache --set-config=compression_level=6
          ccache --zero-stats

      - name: Restore exact 35196552336 prepared source cache
        id: base351_source
        uses: actions/cache/restore@v4
        with:
          path: workspace/touchgrass-a52xq
          key: a52xq-base35196552336-pre-fuse104-${{ runner.os }}-e72ccca-v1

      - name: Restore BT-P2C prepared source cache
        if: steps.base351_source.outputs.cache-hit != 'true'
        id: btp2c_source
        uses: actions/cache/restore@v4
        with:
          path: workspace/touchgrass-a52xq
          key: a52xq-bt-p2c-pre-fuse-${{ runner.os }}-dfd4939-v1

      - name: Verify prepared-source availability
        run: |
          set -Eeuo pipefail
          mkdir -p artifacts release
          chmod +x scripts/*.sh scripts/*.py
          if [ "${{ steps.base351_source.outputs.cache-hit }}" != "true" ] && \
             [ "${{ steps.btp2c_source.outputs.cache-hit }}" != "true" ]; then
            echo "Neither exact baseline cache nor BT-P2C source cache was available" >&2
            exit 1
          fi
          test -d workspace/touchgrass-a52xq/.git

      - name: Audit inherited EEVDF and UCLAMP fixes
        run: |
          set -Eeuo pipefail
          KERNEL=workspace/touchgrass-a52xq
          grep -Fq 'ENQUEUE_INITIAL' "$KERNEL/kernel/sched/sched.h"
          grep -Fq 'eevdf_status_init' "$KERNEL/kernel/sched/fair.c"
          grep -Fq 'sched_eevdf_enabled' "$KERNEL/kernel/sched/fair.c"
          grep -Fq 'U17 uclamp + WALT schedutil compatibility path active' "$KERNEL/kernel/sched/cpufreq_schedutil.c"
          grep -Fq 'unsigned long p_util = uclamp_task(p);' "$KERNEL/kernel/sched/cass.c"
          grep -Fq 'CASS uclamp-aware task demand path is active' "$KERNEL/kernel/sched/cass.c"
          grep -Fq 'sched_util_clamp_min_rt_default' "$KERNEL/kernel/sysctl.c"
          grep -Fq 'uclamp_sync_util_min_rt_default();' "$KERNEL/kernel/sched/core.c"

      - name: Apply FUSE Modern P1
        if: steps.base351_source.outputs.cache-hit != 'true'
        run: |
          set -Eeuo pipefail
          python3 -m py_compile scripts/98_apply_fuse_passthrough_modern_p1.py
          python3 scripts/98_apply_fuse_passthrough_modern_p1.py workspace/touchgrass-a52xq
          git -C workspace/touchgrass-a52xq diff --check

      - name: Apply FUSE Modern P2
        if: steps.base351_source.outputs.cache-hit != 'true'
        run: |
          set -Eeuo pipefail
          python3 -m py_compile scripts/99_apply_fuse_passthrough_modern_p2.py
          python3 scripts/99_apply_fuse_passthrough_modern_p2.py workspace/touchgrass-a52xq
          git -C workspace/touchgrass-a52xq diff --check

      - name: Apply FUSE Modern P3
        if: steps.base351_source.outputs.cache-hit != 'true'
        run: |
          set -Eeuo pipefail
          python3 -m py_compile scripts/101_apply_fuse_passthrough_modern_p3.py
          python3 scripts/101_apply_fuse_passthrough_modern_p3.py workspace/touchgrass-a52xq
          git -C workspace/touchgrass-a52xq diff --check

      - name: Apply FUSE negotiation probe
        if: steps.base351_source.outputs.cache-hit != 'true'
        run: |
          set -Eeuo pipefail
          python3 -m py_compile scripts/102_apply_fuse_negotiation_probe.py
          python3 scripts/102_apply_fuse_negotiation_probe.py workspace/touchgrass-a52xq
          git -C workspace/touchgrass-a52xq diff --check

      - name: Apply FUSE 7.40 INIT compatibility
        if: steps.base351_source.outputs.cache-hit != 'true'
        run: |
          set -Eeuo pipefail
          python3 -m py_compile scripts/103_apply_fuse_740_init_compat.py
          python3 scripts/103_apply_fuse_740_init_compat.py workspace/touchgrass-a52xq
          git -C workspace/touchgrass-a52xq diff --check

      - name: Apply baseline GPU undervolt state
        if: steps.base351_source.outputs.cache-hit != 'true'
        run: |
          set -Eeuo pipefail
          python3 -m py_compile scripts/104_apply_a619_gpu_undervolt_p1.py
          python3 scripts/104_apply_a619_gpu_undervolt_p1.py workspace/touchgrass-a52xq
          python3 -m py_compile scripts/105_apply_a619_gpu_undervolt_p1_diag.py
          python3 scripts/105_apply_a619_gpu_undervolt_p1_diag.py workspace/touchgrass-a52xq
          git -C workspace/touchgrass-a52xq diff --check

      - name: Apply Modern MGLRU P1 from 35196552336
        if: steps.base351_source.outputs.cache-hit != 'true'
        run: |
          set -Eeuo pipefail
          python3 -m py_compile scripts/106_apply_mglru_modern_p1.py
          python3 scripts/106_apply_mglru_modern_p1.py workspace/touchgrass-a52xq
          git -C workspace/touchgrass-a52xq diff --check

      - name: Audit exact pre-Phase104 baseline
        run: |
          set -Eeuo pipefail
          KERNEL=workspace/touchgrass-a52xq
          grep -Fq '#define FUSE_KERNEL_MINOR_VERSION 40' "$KERNEL/include/uapi/linux/fuse.h"
          grep -Fq 'arg->flags2 = 0;' "$KERNEL/fs/fuse/inode.c"
          grep -Fq 'A52 MGLRU Modern P1: Android17 9eff4247' "$KERNEL/mm/vmscan.c"
          grep -Fq 'A52 MGLRU Modern P1: Android a7adb988 per-zone forward progress' "$KERNEL/mm/vmscan.c"
          grep -Fq 'A52 A619 GPU UV P1 diagnostics: persistent sysfs status' "$KERNEL/drivers/gpu/msm/kgsl_gmu.c"
          grep -Fq 'U17 uclamp + WALT schedutil compatibility path active' "$KERNEL/kernel/sched/cpufreq_schedutil.c"
          grep -Fq 'eevdf_status_init' "$KERNEL/kernel/sched/fair.c"

      - name: Save exact 35196552336 prepared source cache
        if: steps.base351_source.outputs.cache-hit != 'true'
        uses: actions/cache/save@v4
        with:
          path: workspace/touchgrass-a52xq
          key: a52xq-base35196552336-pre-fuse104-${{ runner.os }}-e72ccca-v1

      - name: Apply runtime-proven native FUSE 7.40 Phase104
        run: |
          set -Eeuo pipefail
          python3 -m py_compile scripts/104_apply_fuse_740_upstream_passthrough.py
          python3 scripts/104_apply_fuse_740_upstream_passthrough.py workspace/touchgrass-a52xq \
            2>&1 | tee artifacts/fuse-740-native-apply.txt
          KERNEL=workspace/touchgrass-a52xq
          grep -Fq 'arg->flags2 = (u32)(FUSE_PASSTHROUGH_UPSTREAM >> 32);' "$KERNEL/fs/fuse/inode.c"
          grep -Fq 'FUSE_740_PASSTHROUGH_NEGOTIATED' "$KERNEL/fs/fuse/inode.c"
          grep -Fq 'FUSE_740_LEGACY_STACK_COMPAT' "$KERNEL/fs/fuse/backing.c"
          grep -Fq 'FUSE_740_BACKING_OPEN' "$KERNEL/fs/fuse/backing.c"
          grep -Fq 'FUSE_740_PASSTHROUGH_SETUP' "$KERNEL/fs/fuse/passthrough.c"
          git -C "$KERNEL" diff --check

      - name: Build integrated kernel
        run: |
          set -o pipefail
          echo "=== CCACHE BEFORE ==="
          ccache --show-stats || true
          START=$(date +%s)
          ./scripts/57_build_resukisu_susfs_manual_detection.sh \
            2>&1 | tee artifacts/base35196552336-fuse740-build.log
          RC=${PIPESTATUS[0]}
          END=$(date +%s)
          echo "kernel_build_seconds=$((END-START))"
          echo "=== CCACHE AFTER ==="
          ccache --show-stats || true
          exit "$RC"

      - name: Audit integrated image
        run: |
          set -Eeuo pipefail
          KERNEL=workspace/touchgrass-a52xq
          test -s "$IMAGE"
          test -s "$KERNEL/out/vmlinux"
          grep -Fxq 'CONFIG_FUSE_PASSTHROUGH=y' "$KERNEL/out/.config"
          grep -Fxq 'CONFIG_UCLAMP_TASK=y' "$KERNEL/out/.config"
          grep -Fxq 'CONFIG_SCHED_TUNE=y' "$KERNEL/out/.config"
          grep -Fxq 'CONFIG_TCP_CONG_BBR3=y' "$KERNEL/out/.config"
          grep -Fq '#define FUSE_KERNEL_MINOR_VERSION 40' "$KERNEL/include/uapi/linux/fuse.h"
          grep -Fq '#define FUSE_PASSTHROUGH_UPSTREAM' "$KERNEL/include/uapi/linux/fuse.h"
          grep -Fq 'FUSE_740_LEGACY_STACK_COMPAT' "$KERNEL/fs/fuse/backing.c"
          grep -Fq 'A52 MGLRU Modern P1: Android17 9eff4247' "$KERNEL/mm/vmscan.c"
          grep -Fq 'eevdf_status_init' "$KERNEL/kernel/sched/fair.c"
          grep -Fq 'U17 uclamp + WALT schedutil compatibility path active' "$KERNEL/kernel/sched/cpufreq_schedutil.c"
          nm -n "$KERNEL/out/vmlinux" | grep -E ' [Tt] (fuse_backing_open|fuse_backing_close|fuse_passthrough_splice_read|fuse_passthrough_splice_write)$' \
            | tee artifacts/fuse740-vmlinux-symbols.txt

      - name: Check out pinned AnyKernel3
        uses: actions/checkout@v4
        with:
          repository: osm0sis/AnyKernel3
          ref: ${{ env.ANYKERNEL_COMMIT }}
          path: packaging/AnyKernel3
          fetch-depth: 1

      - name: Package integrated flashable ZIP
        run: |
          set -Eeuo pipefail
          mkdir -p release
          cp scripts/09_package_a52xq_anykernel.sh scripts/.package-base351-fuse740.sh
          sed -i "s/grep -Fxq '# CONFIG_KSU_SUSFS is not set'/grep -Fxq 'CONFIG_KSU_SUSFS=y'/" scripts/.package-base351-fuse740.sh
          sed -i 's/SUSFS must remain disabled for this package/Kernel config does not enable SUSFS core/' scripts/.package-base351-fuse740.sh
          python3 - <<'PY'
          from pathlib import Path
          p = Path('scripts/.package-base351-fuse740.sh')
          s = p.read_text()
          anchor = 'dump_boot;\nwrite_boot;'
          if s.count(anchor) != 1:
              raise SystemExit(f'unexpected boot-write anchor count: {s.count(anchor)}')
          s = s.replace(anchor,
              'dump_boot;\n'
              'patch_cmdline "sched_eevdf" "sched_eevdf=1";\n'
              'patch_cmdline "sched_cass" "sched_cass=1";\n'
              'write_boot;', 1)
          p.write_text(s)
          PY
          python3 scripts/88_patch_anykernel_vulkan_renderengine.py scripts/.package-base351-fuse740.sh
          sed -i 's/kernel.string=touchGrass A52xq Linux $TARGET_VERSION + ReSukiSU v4.1.0/kernel.string=touchGrass A52xq Linux $TARGET_VERSION + ReSukiSU v4.1.0 + BASE35196552336 + EEVDF77 + UCLAMP-P2 + MGLRU-MODERN-P1 + FUSE-7.40-NATIVE/' scripts/.package-base351-fuse740.sh
          sed -i 's/ReSukiSU-v4.1.0-BUILD-VERIFIED-FLASHABLE.zip/ReSukiSU-v4.1.0-BASE35196552336-EEVDF-UCLAMP-MGLRU-FUSE740-NATIVE-CLANG11.zip/' scripts/.package-base351-fuse740.sh
          chmod +x scripts/.package-base351-fuse740.sh
          ./scripts/.package-base351-fuse740.sh 4.19.206 "$IMAGE" packaging/AnyKernel3 release
          rm -f scripts/.package-base351-fuse740.sh

      - name: Verify package
        run: |
          set -Eeuo pipefail
          ZIP="$(find release -maxdepth 1 -name '*.zip' -type f | head -1)"
          test -s "$ZIP"
          unzip -p "$ZIP" anykernel.sh | grep -Fq 'patch_cmdline "sched_eevdf" "sched_eevdf=1";'
          unzip -p "$ZIP" anykernel.sh | grep -Fq 'patch_cmdline "sched_cass" "sched_cass=1";'
          unzip -p "$ZIP" anykernel.sh | grep -Fq 'install_vulkan_re_module;'
          unzip -l "$ZIP" | tee artifacts/package-list.txt
          sha256sum "$ZIP" | tee artifacts/package-sha256.txt

      - name: Save compiler cache
        if: always() && steps.ccache_restore.outputs.cache-hit != 'true'
        uses: actions/cache/save@v4
        with:
          path: .ccache
          key: a52xq-base35196552336-fuse740-${{ runner.os }}-v1

      - name: Record diagnostics
        if: always()
        run: |
          mkdir -p artifacts
          if [ -d workspace/touchgrass-a52xq/.git ]; then
            git -C workspace/touchgrass-a52xq status --short > artifacts/kernel-status.txt || true
            git -C workspace/touchgrass-a52xq diff --check > artifacts/diff-check.txt 2>&1 || true
          fi
          ccache --show-stats > artifacts/ccache.txt 2>&1 || true

      - name: Upload integrated flashable ZIP
        uses: actions/upload-artifact@v4
        with:
          name: A52XQ-BASE35196552336-EEVDF-UCLAMP-MGLRU-FUSE740-NATIVE-${{ github.run_number }}
          path: release/
          if-no-files-found: error
          retention-days: 14

      - name: Upload diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: A52XQ-BASE35196552336-FUSE740-DIAG-${{ github.run_number }}
          path: artifacts/
          if-no-files-found: warn
          retention-days: 14
