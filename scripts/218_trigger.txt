Phase 222 paired TouchGrass and GKI librpmb SG/RPMB runtime comparison

Hardware evidence from A52_RAW_RAMOOPS_20260803_235659.zip and Phase 221:
- Phase 220 stopped after librpmb.so opened /dev because CONFIG_CHR_DEV_SG was disabled
- Phase 221 enables CONFIG_CHR_DEV_SG=y and qseecomd now opens /dev/sg0 through /dev/sg8
- qseecomd still exits with status 255 before opening /dev/ion or /dev/qseecom
- therefore the new boundary is SG device inspection or SG_IO communication, not device-node creation

Phase 222 keeps the Phase 221 configuration and adds metadata-only comparison tracing:
- SG ioctl entry and final return code
- SG_IO opcode, command length, direction, transfer length, timeout and UFS LUN
- SCSI, host and driver status, residual bytes, sense length, sense key, ASC and ASCQ
- exact qseecomd task filter
- no CDB payload bytes beyond opcode, no transfer buffers, no RPMB frames, no keys and no userspace memory

Later progress coverage is also retained:
- bounded exec, exec-return and exit milestones for init, service managers, storage, graphics and security services
- existing QSEECom, ION, all-open, exit and 60-second heartbeat tracing remains enabled
- the same patcher supports a TouchGrass printk reference backend using TGSG 222 and TGBOOT 222
- the comparison tool reports the first differing ioctl, opcode, LUN, result or sense status

No SG, UFS, QSEECom, ION, SCM, KeyMint, DTB, DTBO, ramdisk, clock, regulator, power or userspace behavior is modified. Existing three-copy R48 transport, 48 Reed-Solomon parity symbols, CRC32C, decoder tests, source reconstruction, compiler checks, boot-image repack invariants and candidate audits remain mandatory.