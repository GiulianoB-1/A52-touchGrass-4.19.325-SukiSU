name: 89 - supersede GPU P2 Vulkan RE build

on:
  push:
    branches:
      - agent/a52-cass-p2-gpu-p2-vulkan-re-v1
    paths:
      - .github/workflows/89-a52-cass-p2-a619-gpu-p2-vulkan-re-clang11.yml
  workflow_dispatch:

concurrency:
  group: a52-cass-p2-gpu-p2-vulkan-re-${{ github.ref }}
  cancel-in-progress: true

jobs:
  supersede:
    runs-on: ubuntu-22.04
    steps:
      - run: echo "Superseded by GPU Phase3 integration branch."
