name: 68 - A52 AutoFDO A1 retired

on:
  push:
    branches:
      - agent/a52-autofdo-a1-kbuild-4.19.206-v1
    paths:
      - .github/workflows/68-a52-autofdo-a1-kbuild.yml
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: a52-autofdo-a1-${{ github.ref }}
  cancel-in-progress: true

jobs:
  retired:
    runs-on: ubuntu-22.04
    steps:
      - run: echo "A1 standalone validation retired; ETM training path continues in A2."
