name: 166 - A52 QSEECOM TA heap 19

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - .github/workflows/166-a52-qseecom-ta-heap19.yml

permissions:
  contents: read

jobs:
  register-feature-workflow:
    runs-on: ubuntu-latest
    steps:
      - name: Register Workflow 166
        run: |
          echo "Workflow 166 is registered on main."
          echo "Dispatch it with ref agent/a52-qseecom-ta-heap19 to execute the full feature-branch definition."
