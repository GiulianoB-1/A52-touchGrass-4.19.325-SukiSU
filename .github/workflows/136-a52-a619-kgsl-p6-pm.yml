name: 136 - A52 A619 KGSL P6 + Power Management

on:
  push:
    branches:
      - agent/a52-opt-p1-mglru-cass-uclamp-sugov
    paths:
      - .github/workflows/136-a52-a619-kgsl-p6-pm.yml
      - scripts/134_apply_bpf_devmap_rndis_clean_fix.py
      - scripts/135_remove_project_diagnostics.py
      - scripts/136_apply_a619_kgsl_p6_pm.py
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: a52-a619-kgsl-p6-pm-p136-${{ github.ref }}
  cancel-in-progress: true

env:
  JOBS: '4'
  CCACHE_DIR: ${{ github.workspace }}/.ccache
  CCACHE_MAXSIZE: 5G
  IMAGE: artifacts/Image-touchgrass-4.19.206-resukisu-v4.1.0-susfs-v1.4.2-manual-core
  ANYKERNEL_COMMIT: 1c9a500dd4aa8081952523126e97eb155aed941b

jobs:
  build:
    name: Build exact 35780015630 baseline + A619 KGSL P6 + PM P136
    runs-on: ubuntu-22.04
    timeout-minutes: 360

    steps:
      - name: Check out exact run 35780015630 project baseline
        uses: actions/checkout@v4
        with:
          ref: 541b9f923ab2a04c6f536ba9fe2b1e9976e28bdb

      - name: Free runner disk space
        run: |
          sudo rm -rf /usr/local/lib/android /usr/share/dotnet /opt/ghc || true
          df -h

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            git curl xz-utils zip unzip make gcc g++ bc bison flex ccache patch \
            libssl-dev libelf-dev python3 rsync cpio lz4 binutils file tar

      - name: Restore compiler cache
        id: ccache_restore
        uses: actions/cache/restore@v4
        with:
          path: .ccache
          key: a52xq-f2fs325-p1-${{ runner.os }}-${{ github.sha }}
          restore-keys: |
            a52xq-effp1-recorder-${{ runner.os }}-
            a52xq-f2fs325-p1-${{ runner.os }}-

      - name: Configure compiler cache
        run: |
          mkdir -p "$CCACHE_DIR"
          ccache --max-size "$CCACHE_MAXSIZE"
          ccache --set-config=compression=true
          ccache --set-config=compression_level=6
          ccache --zero-stats

      - name: Restore prepared P134 feature source cache
        id: p134_source
        uses: actions/cache/restore@v4
        with:
          path: workspace/touchgrass-a52xq
          key: a52xq-base35633887441-p134-src-v1-${{ runner.os }}-${{ hashFiles('scripts/118_apply_f2fs_stable_325_p1.sh', 'scripts/119_apply_f2fs_gc_merge_p2.py', 'scripts/121_apply_f2fs_inmem_curseg_p3a.py', 'scripts/122_apply_f2fs_atgc_p3b.py', 'scripts/123_enable_f2fs_atgc_p3c.py', 'scripts/124_apply_f2fs_atgc_p4.py', 'scripts/125_apply_f2fs_block_age_p5a.py', 'scripts/126_enable_f2fs_block_age_p5b.py', 'scripts/127_apply_f2fs_gc_cp_p6.py', 'scripts/128_apply_f2fs_correctness_p7.py', 'scripts/129_apply_bpf_bootfix_p1.py', 'scripts/134_apply_bpf_devmap_rndis_clean_fix.py') }}

      - name: Restore exact 35461116688 pre-build kernel source
        if: steps.p134_source.outputs.cache-hit != 'true'
        id: prebuild_source
        uses: actions/cache/restore@v4
        with:
          path: workspace/touchgrass-a52xq
          key: a52xq-effp1-recorder-prebuild-${{ runner.os }}-${{ hashFiles('scripts/104_apply_fuse_740_upstream_passthrough.py', 'scripts/106_apply_mglru_modern_p1.py', 'scripts/106_apply_mglru_modern_p1_core.py', 'scripts/107_apply_mglru_modern_p2.py', 'scripts/108_apply_mglru_modern_p3.py', 'scripts/109_apply_eevdf_efficiency_p1.py', 'scripts/109b_finalize_eevdf_efficiency_p1.py', 'scripts/110_apply_uclamp_efficiency_p1.py', 'scripts/110b_finalize_uclamp_efficiency_p1.py', 'scripts/111_apply_mglru_power_diag_p1.py', 'scripts/112_apply_mglru_power_recorder_p2.py', 'scripts/115_apply_a52_efficiency_p1.py', 'scripts/116_apply_a52_scheduler_recorder.py', 'scripts/117_migrate_mglru_trace_to_proc.py') }}

      - name: Require exact successful baseline cache
        if: steps.p134_source.outputs.cache-hit != 'true'
        run: |
          set -Eeuo pipefail
          echo "cache-hit=${{ steps.prebuild_source.outputs.cache-hit }}"
          if [ "${{ steps.prebuild_source.outputs.cache-hit }}" != "true" ]; then
            echo "::error::Exact pre-build source cache from baseline 35461116688 was not restored."
            exit 1
          fi
          test -d workspace/touchgrass-a52xq/.git
          grep -Fq '#define FUSE_KERNEL_MINOR_VERSION 40' workspace/touchgrass-a52xq/include/uapi/linux/fuse.h
          grep -Fq 'A52 MGLRU Efficiency P4: clear seed PTE before look-around bailouts' workspace/touchgrass-a52xq/mm/vmscan.c
          grep -Fq 'A52 CASS P3: uclamp-fit + sync-waker + no task double-count active' workspace/touchgrass-a52xq/kernel/sched/cass.c
          grep -Fq 'A52 A619 GPU UV P3: 565 MHz one ARC corner' workspace/touchgrass-a52xq/drivers/gpu/msm/kgsl_gmu.c

      - name: Validate prepared P134 feature source cache
        if: steps.p134_source.outputs.cache-hit == 'true'
        run: |
          set -Eeuo pipefail
          echo "P134 source cache hit: skipping F2FS/BPF preparation phases"
          KERNEL=workspace/touchgrass-a52xq
          test -d "$KERNEL/.git"
          grep -Fq '#define FUSE_KERNEL_MINOR_VERSION 40' "$KERNEL/include/uapi/linux/fuse.h"
          grep -Fq 'A52 MGLRU Efficiency P4: clear seed PTE before look-around bailouts' "$KERNEL/mm/vmscan.c"
          grep -Fq 'A52 A619 GPU UV P3: 565 MHz one ARC corner' "$KERNEL/drivers/gpu/msm/kgsl_gmu.c"
          grep -Fq 'static void dev_map_hash_remove_netdev' "$KERNEL/kernel/bpf/devmap.c"
          grep -Fq 'dev->bulkq = __alloc_percpu_gfp(sizeof(*dev->bulkq)' "$KERNEL/kernel/bpf/devmap.c"
          ! grep -Fq 'a52_rndis_reftrace' "$KERNEL/net/core/dev.c"
          ! grep -Fq 'a52_rndis_fibtrace' "$KERNEL/net/core/dev.c"

      - name: Prepare workspace
        run: |
          set -Eeuo pipefail
          mkdir -p artifacts release
          chmod +x scripts/*.sh scripts/*.py

      - name: Apply compatible F2FS stable fixes through 4.19.325
        if: steps.p134_source.outputs.cache-hit != 'true'
        run: |
          set -Eeuo pipefail
          ./scripts/118_apply_f2fs_stable_325_p1.sh \
            workspace/touchgrass-a52xq \
            artifacts/f2fs-stable325-p1.txt \
            artifacts/f2fs-stable325-patches
          cat artifacts/f2fs-stable325-p1.txt
          git -C workspace/touchgrass-a52xq diff --stat | tee artifacts/f2fs-stable325-diffstat.txt
          git -C workspace/touchgrass-a52xq diff -- fs/f2fs include/linux/f2fs_fs.h \
            > artifacts/f2fs-stable325-p1.diff || true

      - name: Apply F2FS gc_merge P2
        if: steps.p134_source.outputs.cache-hit != 'true'
        run: |
          set -Eeuo pipefail
          python3 scripts/119_apply_f2fs_gc_merge_p2.py workspace/touchgrass-a52xq
          git -C workspace/touchgrass-a52xq diff --check
          git -C workspace/touchgrass-a52xq diff --stat | tee artifacts/f2fs-gcmerge-p2-diffstat.txt
          git -C workspace/touchgrass-a52xq diff -- fs/f2fs > artifacts/f2fs-gcmerge-p2.diff
          grep -Fq 'F2FS_MOUNT_GC_MERGE' workspace/touchgrass-a52xq/fs/f2fs/f2fs.h
          grep -Fq 'waitqueue_active(fggc_wq)' workspace/touchgrass-a52xq/fs/f2fs/gc.c
          grep -Fq 'prepare_to_wait(&sbi->gc_thread->fggc_wq' workspace/touchgrass-a52xq/fs/f2fs/segment.c
          grep -Fq 'f2fs_submit_merged_write(sbi, DATA)' workspace/touchgrass-a52xq/fs/f2fs/segment.c
          grep -Fq '"gc_merge"' workspace/touchgrass-a52xq/fs/f2fs/super.c

      - name: Apply F2FS inmem curseg P3A
        if: steps.p134_source.outputs.cache-hit != 'true'
        run: |
          set -Eeuo pipefail
          python3 scripts/121_apply_f2fs_inmem_curseg_p3a.py \
            workspace/touchgrass-a52xq \
            artifacts/f2fs-inmem-p3a
          git -C workspace/touchgrass-a52xq diff --check
          git -C workspace/touchgrass-a52xq diff --stat | tee artifacts/f2fs-inmem-p3a-diffstat.txt
          grep -Fq 'NR_CURSEG_INMEM_TYPE' workspace/touchgrass-a52xq/fs/f2fs/f2fs.h
          grep -Fq 'CURSEG_COLD_DATA_PINNED = NR_PERSISTENT_LOG' workspace/touchgrass-a52xq/fs/f2fs/f2fs.h
          grep -Fq 'void f2fs_save_inmem_curseg' workspace/touchgrass-a52xq/fs/f2fs/segment.c
          grep -Fq '__allocate_new_segment' workspace/touchgrass-a52xq/fs/f2fs/segment.c
          grep -Fq 'active_logs != NR_CURSEG_PERSIST_TYPE' workspace/touchgrass-a52xq/fs/f2fs/super.c
          ! grep -Rqs 'CURSEG_ALL_DATA_ATGC\|F2FS_MOUNT_ATGC\|GC_IDLE_AT' workspace/touchgrass-a52xq/fs/f2fs

      - name: Snapshot booted P3A F2FS source
        if: steps.p134_source.outputs.cache-hit != 'true'
        run: |
          set -Eeuo pipefail
          mkdir -p artifacts/pre-atgc-p3b/fs artifacts/pre-atgc-p3b/include/trace/events
          cp -a workspace/touchgrass-a52xq/fs/f2fs artifacts/pre-atgc-p3b/fs/
          cp -a workspace/touchgrass-a52xq/include/trace/events/f2fs.h artifacts/pre-atgc-p3b/include/trace/events/

      - name: Apply F2FS ATGC P3B
        if: steps.p134_source.outputs.cache-hit != 'true'
        run: |
          set -Eeuo pipefail
          python3 scripts/122_apply_f2fs_atgc_p3b.py \
            workspace/touchgrass-a52xq \
            artifacts/f2fs-atgc-p3b
          git -C workspace/touchgrass-a52xq diff --check
          cat artifacts/f2fs-atgc-p3b/report.txt
          grep -Fq 'manual Samsung ATGC adaptation=PASS' artifacts/f2fs-atgc-p3b/report.txt
          grep -Fq 'ATGC default=off' artifacts/f2fs-atgc-p3b/report.txt

      - name: Enable ATGC at initial mount P3C
        if: steps.p134_source.outputs.cache-hit != 'true'
        run: |
          set -Eeuo pipefail
          python3 scripts/123_enable_f2fs_atgc_p3c.py \
            workspace/touchgrass-a52xq \
            artifacts/f2fs-atgc-p3c
          git -C workspace/touchgrass-a52xq diff --check
          cat artifacts/f2fs-atgc-p3c/report.txt
          grep -Fq 'ATGC default=on at initial mount only' artifacts/f2fs-atgc-p3c/report.txt

      - name: Apply newer ATGC refinements P4
        if: steps.p134_source.outputs.cache-hit != 'true'
        run: |
          set -Eeuo pipefail
          python3 scripts/124_apply_f2fs_atgc_p4.py \
            workspace/touchgrass-a52xq \
            artifacts/f2fs-atgc-p4
          git -C workspace/touchgrass-a52xq diff --check
          cat artifacts/f2fs-atgc-p4/report.txt
          grep -Fq 'dynamic_atgc_reenable=6efc3a05e613' artifacts/f2fs-atgc-p4/report.txt
          grep -Fq 'atgc_enabled_sysfs=6f092b55e1ad' artifacts/f2fs-atgc-p4/report.txt
          grep -Fq 'skip_empty_gc_sections=dccd324fa9bd' artifacts/f2fs-atgc-p4/report.txt

      - name: Apply block-age extent cache P5A
        if: steps.p134_source.outputs.cache-hit != 'true'
        run: |
          set -Eeuo pipefail
          python3 scripts/125_apply_f2fs_block_age_p5a.py \
            workspace/touchgrass-a52xq \
            artifacts/f2fs-block-age-p5a
          git -C workspace/touchgrass-a52xq diff --check
          cat artifacts/f2fs-block-age-p5a/report.txt
          grep -Fq 'feature=71644dff4811' artifacts/f2fs-block-age-p5a/report.txt
          grep -Fq 'fix_age_math=b03a41a495df' artifacts/f2fs-block-age-p5a/report.txt
          grep -Fq 'activation=OFF by default' artifacts/f2fs-block-age-p5a/report.txt

      - name: Enable block-age extent cache P5B
        if: steps.p134_source.outputs.cache-hit != 'true'
        run: |
          set -Eeuo pipefail
          python3 scripts/126_enable_f2fs_block_age_p5b.py \
            workspace/touchgrass-a52xq \
            artifacts/f2fs-block-age-p5b
          git -C workspace/touchgrass-a52xq diff --check
          cat artifacts/f2fs-block-age-p5b/report.txt
          grep -Fq 'age_extent_cache default=on at initial mount' artifacts/f2fs-block-age-p5b/report.txt
          grep -Fq 'observability=enabled,tree_count,node_count,allocated_data_blocks' artifacts/f2fs-block-age-p5b/report.txt

      - name: Apply GC/segment/checkpoint modernization P6
        if: steps.p134_source.outputs.cache-hit != 'true'
        run: |
          set -Eeuo pipefail
          python3 scripts/127_apply_f2fs_gc_cp_p6.py \
            workspace/touchgrass-a52xq \
            artifacts/f2fs-gc-cp-p6
          git -C workspace/touchgrass-a52xq diff --check
          cat artifacts/f2fs-gc-cp-p6/report.txt
          grep -Fq 'checkpoint_freeze_fix=d50dfc0c7df7 adapted' artifacts/f2fs-gc-cp-p6/report.txt
          grep -Fq 'ioctl_gc_victim_fix=e9a844f6e487 adapted' artifacts/f2fs-gc-cp-p6/report.txt
          grep -Fq 'lfs_map_blocks_balance_fix=1005a3ca28e9' artifacts/f2fs-gc-cp-p6/report.txt

      - name: Apply recent correctness/data-integrity P7
        if: steps.p134_source.outputs.cache-hit != 'true'
        run: |
          set -Eeuo pipefail
          python3 scripts/128_apply_f2fs_correctness_p7.py \
            workspace/touchgrass-a52xq \
            artifacts/f2fs-correctness-p7
          git -C workspace/touchgrass-a52xq diff --check
          cat artifacts/f2fs-correctness-p7/report.txt
          grep -Fq 'nat_flag_lock_fix=238e14eb7226 adapted' artifacts/f2fs-correctness-p7/report.txt
          grep -Fq 'node_mark_fix=019f9dda7f66+c3e238bd1f56 adapted-to-pages' artifacts/f2fs-correctness-p7/report.txt
          grep -Fq 'write_end_io_uaf_fix=ce2739e482bc+2d9c4a4ed4ee adapted-to-pages' artifacts/f2fs-correctness-p7/report.txt

      - name: Apply BPF boot reliability P1
        if: steps.p134_source.outputs.cache-hit != 'true'
        run: |
          set -Eeuo pipefail
          python3 -m py_compile scripts/129_apply_bpf_bootfix_p1.py
          python3 scripts/129_apply_bpf_bootfix_p1.py \
            workspace/touchgrass-a52xq \
            artifacts/bpf-bootfix-p1
          cat artifacts/bpf-bootfix-p1/report.txt
          KERNEL=workspace/touchgrass-a52xq
          git -C "$KERNEL" diff --check
          ! grep -RFn 'REG_LIVE_DONE' "$KERNEL/include/linux/bpf_verifier.h" "$KERNEL/kernel/bpf/verifier.c"
          ! grep -Fq 'struct idpair' "$KERNEL/kernel/bpf/verifier.c"
          grep -Fq 'struct bpf_id_pair *idmap' "$KERNEL/kernel/bpf/verifier.c"
          grep -Fq 'i < BPF_ID_MAP_SIZE' "$KERNEL/kernel/bpf/verifier.c"
          grep -Fq 'if (env->explore_alu_limits)' "$KERNEL/kernel/bpf/verifier.c"
          grep -Fq 'ctx_access = true;' "$KERNEL/kernel/bpf/verifier.c"
          grep -Fq 'ctx_access = BPF_CLASS(insn->code) == BPF_STX;' "$KERNEL/kernel/bpf/verifier.c"
          grep -Fq 'ptype = BPF_PROG_TYPE_SK_MSG;' "$KERNEL/kernel/bpf/syscall.c"
          grep -Fq 'ptype = BPF_PROG_TYPE_SK_SKB;' "$KERNEL/kernel/bpf/syscall.c"
          git -C "$KERNEL" diff -- include/linux/bpf_verifier.h kernel/bpf/verifier.c kernel/bpf/syscall.c \
            > artifacts/bpf-bootfix-p1.diff

      - name: Apply clean BPF DEVMAP RNDIS production fix P134
        if: steps.p134_source.outputs.cache-hit != 'true'
        run: |
          set -Eeuo pipefail
          curl -fsSL \
            "https://raw.githubusercontent.com/${{ github.repository }}/${{ github.sha }}/scripts/134_apply_bpf_devmap_rndis_clean_fix.py" \
            -o /tmp/134_apply_bpf_devmap_rndis_clean_fix.py
          python3 -m py_compile /tmp/134_apply_bpf_devmap_rndis_clean_fix.py
          python3 /tmp/134_apply_bpf_devmap_rndis_clean_fix.py \
            workspace/touchgrass-a52xq \
            artifacts/bpf-devmap-rndis-clean-p134
          cat artifacts/bpf-devmap-rndis-clean-p134/report.txt

          KERNEL=workspace/touchgrass-a52xq
          git -C "$KERNEL" diff --check

          # Permanent fix must be present.
          grep -Fq 'static void dev_map_hash_remove_netdev' "$KERNEL/kernel/bpf/devmap.c"
          grep -Fq 'dev_map_hash_remove_netdev(dtab, netdev);' "$KERNEL/kernel/bpf/devmap.c"
          grep -Fq 'dev->bulkq = __alloc_percpu_gfp(sizeof(*dev->bulkq)' "$KERNEL/kernel/bpf/devmap.c"
          grep -Fq 'head = &dtab->dev_index_head[i];' "$KERNEL/kernel/bpf/devmap.c"
          grep -Fq 'hlist_for_each_entry_safe(dev, next, head, index_hlist)' "$KERNEL/kernel/bpf/devmap.c"

          # No RNDIS diagnostic instrumentation in the production phase.
          ! grep -Fq 'a52_rndis_reftrace' "$KERNEL/include/linux/netdevice.h"
          ! grep -Fq 'a52_rndis_reftrace' "$KERNEL/net/core/dev.c"
          ! grep -Fq 'a52_rndis_fibtrace' "$KERNEL/include/linux/netdevice.h"
          ! grep -Fq 'a52_rndis_fibtrace' "$KERNEL/net/core/dev.c"
          ! grep -Fq 'a52_rndis_reftrace' "$KERNEL/drivers/platform/msm/ipa/ipa_clients/rndis_ipa.c"
          ! grep -Fq 'a52_rndis_fibtrace' "$KERNEL/drivers/platform/msm/ipa/ipa_clients/rndis_ipa.c"

          git -C "$KERNEL" diff -- kernel/bpf/devmap.c \
            > artifacts/bpf-devmap-rndis-clean-p134.diff

      - name: Save prepared P134 feature source cache
        if: steps.p134_source.outputs.cache-hit != 'true'
        uses: actions/cache/save@v4
        with:
          path: workspace/touchgrass-a52xq
          key: a52xq-base35633887441-p134-src-v1-${{ runner.os }}-${{ hashFiles('scripts/118_apply_f2fs_stable_325_p1.sh', 'scripts/119_apply_f2fs_gc_merge_p2.py', 'scripts/121_apply_f2fs_inmem_curseg_p3a.py', 'scripts/122_apply_f2fs_atgc_p3b.py', 'scripts/123_enable_f2fs_atgc_p3c.py', 'scripts/124_apply_f2fs_atgc_p4.py', 'scripts/125_apply_f2fs_block_age_p5a.py', 'scripts/126_enable_f2fs_block_age_p5b.py', 'scripts/127_apply_f2fs_gc_cp_p6.py', 'scripts/128_apply_f2fs_correctness_p7.py', 'scripts/129_apply_bpf_bootfix_p1.py', 'scripts/134_apply_bpf_devmap_rndis_clean_fix.py') }}

      - name: Remove all project diagnostics P135
        run: |
          set -Eeuo pipefail
          curl -fsSL \
            "https://raw.githubusercontent.com/${{ github.repository }}/${{ github.sha }}/scripts/135_remove_project_diagnostics.py" \
            -o /tmp/135_remove_project_diagnostics.py
          python3 -m py_compile /tmp/135_remove_project_diagnostics.py
          python3 /tmp/135_remove_project_diagnostics.py \
            workspace/touchgrass-a52xq \
            artifacts/production-nodiag-p135
          cat artifacts/production-nodiag-p135/report.txt

          KERNEL=workspace/touchgrass-a52xq
          git -C "$KERNEL" diff --check

          # Diagnostic interfaces/hooks must be gone.
          ! grep -RFn 'a52_sched_diag' "$KERNEL/kernel/sched"
          ! grep -Fq 'lru_gen_trace' "$KERNEL/mm/vmscan.c"
          ! grep -Fq 'mglru_rec_' "$KERNEL/mm/vmscan.c"
          ! grep -Fq 'mglru_diag_' "$KERNEL/mm/vmscan.c"
          ! grep -RFn 'FUSE_NEGOTIATION_PROBE\|FUSE_740_' "$KERNEL/fs/fuse"
          ! grep -RFn 'a52_gpu_uv_p1_status\|a52_gpu_uv_p2_status\|a52_gpu_uv_p3_status' "$KERNEL/drivers/gpu/msm"
          ! grep -RFn 'dev_attr_a52_gpu_uv_p1\|dev_attr_a52_gpu_uv_p2\|dev_attr_a52_gpu_uv_p3' "$KERNEL/drivers/gpu/msm"
          ! grep -RFn 'a52_rndis_reftrace\|a52_rndis_fibtrace' \
            "$KERNEL/include/linux/netdevice.h" "$KERNEL/net/core/dev.c" \
            "$KERNEL/drivers/platform/msm/ipa/ipa_clients/rndis_ipa.c"

          # Production features must remain.
          grep -Fq 'A52 MGLRU Efficiency P4: clear seed PTE before look-around bailouts' "$KERNEL/mm/vmscan.c"
          grep -Fq 'cass_walt_cpu_util(int cpu, int this_cpu, bool sync)' "$KERNEL/kernel/sched/cass.c"
          grep -Fq 'if (likely(!uclamp_is_used()))' "$KERNEL/kernel/sched/sched.h"
          grep -Fq 'delta_ns = time - j_sg_cpu->last_update;' "$KERNEL/kernel/sched/cpufreq_schedutil.c"
          grep -Fq '#define FUSE_KERNEL_MINOR_VERSION 40' "$KERNEL/include/uapi/linux/fuse.h"
          grep -Fq 'FUSE_PASSTHROUGH_UPSTREAM' "$KERNEL/include/uapi/linux/fuse.h"
          grep -Fq 'A52 A619 GPU UV P3: 565 MHz one ARC corner' "$KERNEL/drivers/gpu/msm/kgsl_gmu.c"
          grep -Fq 'static void dev_map_hash_remove_netdev' "$KERNEL/kernel/bpf/devmap.c"

          git -C "$KERNEL" diff -- \
            mm/vmscan.c kernel/sched drivers/gpu/msm fs/fuse fs/f2fs/sysfs.c \
            > artifacts/production-nodiag-p135.diff

      - name: Apply A619 KGSL P6 + power-management modernization
        run: |
          set -Eeuo pipefail
          curl -fsSL \
            "https://raw.githubusercontent.com/${{ github.repository }}/${{ github.sha }}/scripts/136_apply_a619_kgsl_p6_pm.py" \
            -o /tmp/136_apply_a619_kgsl_p6_pm.py
          python3 -m py_compile /tmp/136_apply_a619_kgsl_p6_pm.py
          python3 /tmp/136_apply_a619_kgsl_p6_pm.py workspace/touchgrass-a52xq \
            | tee artifacts/a619-kgsl-p6-pm.txt

          KERNEL=workspace/touchgrass-a52xq
          git -C "$KERNEL" diff --check
          grep -Fq 'A619 KGSL P6 PM: Qualcomm 45d7e571a332 + ca1cbeedfcd6' "$KERNEL/drivers/gpu/msm/kgsl_pwrscale.c"
          grep -Fq 'IS_ERR_OR_NULL(devfreq)' "$KERNEL/drivers/gpu/msm/kgsl_pwrscale.c"
          grep -Fq 'A619 KGSL P6 PM: Qualcomm eff8f6e07da7' "$KERNEL/drivers/gpu/msm/kgsl_pwrctrl.c"
          grep -Fq 'qcom,initial-min-pwrlevel' "$KERNEL/drivers/gpu/msm/adreno.c"
          grep -Fq 'A619 KGSL P6 PM: Qualcomm dde4355ea92d' "$KERNEL/drivers/gpu/msm/adreno_a6xx.c"
          grep -Fq 'A6XX_UCHE_GBIF_GX_CONFIG, 0x70000' "$KERNEL/drivers/gpu/msm/adreno_a6xx.c"

          # Preserve the already-tested A619 modernization and production cleanup.
          grep -Fq 'struct adreno_rb_shadow' "$KERNEL/drivers/gpu/msm/kgsl.h"
          grep -Fq 'A619 GPU P4: Qualcomm 96f7537ccfcd' "$KERNEL/drivers/gpu/msm/adreno_a6xx_preempt.c"
          grep -Fq 'A52 A619 GPU UV P3: 565 MHz one ARC corner' "$KERNEL/drivers/gpu/msm/kgsl_gmu.c"
          ! grep -RFn 'a52_gpu_uv_p1_status\|a52_gpu_uv_p2_status\|a52_gpu_uv_p3_status' "$KERNEL/drivers/gpu/msm"

          git -C "$KERNEL" diff -- drivers/gpu/msm \
            > artifacts/a619-kgsl-p6-pm.diff
          git -C "$KERNEL" diff --stat | tee artifacts/a619-kgsl-p6-pm-diffstat.txt

      - name: Build integrated kernel
        run: |
          set -o pipefail
          echo '=== CCACHE BEFORE ==='
          ccache --show-stats || true
          START=$(date +%s)
          ./scripts/57_build_resukisu_susfs_manual_detection.sh \
            2>&1 | tee artifacts/a619-kgsl-p6-pm-build.log
          RC=${PIPESTATUS[0]}
          END=$(date +%s)
          echo "kernel_build_seconds=$((END-START))"
          echo '=== CCACHE AFTER ==='
          ccache --show-stats || true
          exit "$RC"

      - name: Audit integrated image
        run: |
          set -Eeuo pipefail
          KERNEL=workspace/touchgrass-a52xq
          test -s "$IMAGE"
          test -s "$KERNEL/out/vmlinux"
          grep -Fxq 'CONFIG_F2FS_FS=y' "$KERNEL/out/.config"
          grep -Fxq 'CONFIG_FUSE_PASSTHROUGH=y' "$KERNEL/out/.config"
          grep -Fxq 'CONFIG_UCLAMP_TASK=y' "$KERNEL/out/.config"
          grep -Fxq 'CONFIG_BPF=y' "$KERNEL/out/.config"
          grep -Fxq 'CONFIG_BPF_SYSCALL=y' "$KERNEL/out/.config"
          grep -Fxq 'CONFIG_BPF_JIT=y' "$KERNEL/out/.config"
          grep -Fxq 'CONFIG_BPF_JIT_ALWAYS_ON=y' "$KERNEL/out/.config"
          ! grep -RFn 'REG_LIVE_DONE' "$KERNEL/include/linux/bpf_verifier.h" "$KERNEL/kernel/bpf/verifier.c"
          ! grep -Fq 'struct idpair' "$KERNEL/kernel/bpf/verifier.c"
          grep -Fq 'ctx_access = true;' "$KERNEL/kernel/bpf/verifier.c"
          grep -Fq 'ctx_access = BPF_CLASS(insn->code) == BPF_STX;' "$KERNEL/kernel/bpf/verifier.c"
          grep -Fq 'ptype = BPF_PROG_TYPE_SK_MSG;' "$KERNEL/kernel/bpf/syscall.c"
          grep -Fq 'ptype = BPF_PROG_TYPE_SK_SKB;' "$KERNEL/kernel/bpf/syscall.c"
          grep -Fq 'static void dev_map_hash_remove_netdev' "$KERNEL/kernel/bpf/devmap.c"
          grep -Fq 'dev_map_hash_remove_netdev(dtab, netdev);' "$KERNEL/kernel/bpf/devmap.c"
          grep -Fq 'dev->bulkq = __alloc_percpu_gfp(sizeof(*dev->bulkq)' "$KERNEL/kernel/bpf/devmap.c"
          grep -Fq 'head = &dtab->dev_index_head[i];' "$KERNEL/kernel/bpf/devmap.c"
          grep -Fq 'hlist_for_each_entry_safe(dev, next, head, index_hlist)' "$KERNEL/kernel/bpf/devmap.c"
          ! grep -Fq 'a52_rndis_reftrace' "$KERNEL/include/linux/netdevice.h"
          ! grep -Fq 'a52_rndis_reftrace' "$KERNEL/net/core/dev.c"
          ! grep -Fq 'a52_rndis_fibtrace' "$KERNEL/include/linux/netdevice.h"
          ! grep -Fq 'a52_rndis_fibtrace' "$KERNEL/net/core/dev.c"
          ! grep -Fq 'a52_rndis_reftrace' "$KERNEL/drivers/platform/msm/ipa/ipa_clients/rndis_ipa.c"
          ! grep -Fq 'a52_rndis_fibtrace' "$KERNEL/drivers/platform/msm/ipa/ipa_clients/rndis_ipa.c"
          grep -Fq 'dtab->map.map_type == BPF_MAP_TYPE_DEVMAP_HASH' "$KERNEL/kernel/bpf/devmap.c"
          grep -Fq 'dev->bulkq = __alloc_percpu_gfp(sizeof(*dev->bulkq)' "$KERNEL/kernel/bpf/devmap.c"
          grep -Fq 'applied=' artifacts/f2fs-stable325-p1.txt
          grep -Fq 'processed=34' artifacts/f2fs-stable325-p1.txt
          grep -Fq 'F2FS_MOUNT_GC_MERGE' "$KERNEL/fs/f2fs/f2fs.h"
          grep -Fq 'fggc_wq' "$KERNEL/fs/f2fs/gc.h"
          grep -Fq 'waitqueue_active(fggc_wq)' "$KERNEL/fs/f2fs/gc.c"
          grep -Fq 'f2fs_submit_merged_write(sbi, DATA)' "$KERNEL/fs/f2fs/segment.c"
          grep -Fq 'Opt_gc_merge' "$KERNEL/fs/f2fs/super.c"
          grep -Fq 'NR_CURSEG_INMEM_TYPE' "$KERNEL/fs/f2fs/f2fs.h"
          grep -Fq 'NR_CURSEG_PERSIST_TYPE' "$KERNEL/fs/f2fs/f2fs.h"
          grep -Fq 'CURSEG_COLD_DATA_PINNED = NR_PERSISTENT_LOG' "$KERNEL/fs/f2fs/f2fs.h"
          grep -Fq 'void f2fs_save_inmem_curseg' "$KERNEL/fs/f2fs/segment.c"
          grep -Fq 'void f2fs_restore_inmem_curseg' "$KERNEL/fs/f2fs/segment.c"
          grep -Fq '__allocate_new_segment' "$KERNEL/fs/f2fs/segment.c"
          grep -Fq 'active_logs != NR_CURSEG_PERSIST_TYPE' "$KERNEL/fs/f2fs/super.c"
          grep -Fq 'F2FS_MOUNT_ATGC' "$KERNEL/fs/f2fs/f2fs.h"
          grep -Fq 'CURSEG_ALL_DATA_ATGC' "$KERNEL/fs/f2fs/f2fs.h"
          grep -Fq 'GC_IDLE_AT' "$KERNEL/fs/f2fs/f2fs.h"
          grep -Fq 'GC_AT' "$KERNEL/fs/f2fs/segment.h"
          grep -Fq 'AT_SSR' "$KERNEL/fs/f2fs/segment.h"
          grep -Fq 'atgc_age_threshold' "$KERNEL/fs/f2fs/sysfs.c"
          grep -Fq 'Opt_atgc' "$KERNEL/fs/f2fs/super.c"
          grep -Fq 'P3C: enable ATGC only during the initial mount' "$KERNEL/fs/f2fs/super.c"
          grep -Fq 'set_opt(sbi, ATGC);' "$KERNEL/fs/f2fs/super.c"
          grep -Fq 'f2fs_reinit_atgc_curseg' "$KERNEL/fs/f2fs/checkpoint.c"
          grep -Fq 'reenabled age threshold GC' "$KERNEL/fs/f2fs/segment.c"
          grep -Fq 'fio.sbi->gc_mode != GC_URGENT' "$KERNEL/fs/f2fs/gc.c"
          grep -Fq '__is_valid_data_blkaddr(fio->old_blkaddr)' "$KERNEL/fs/f2fs/segment.c"
          grep -Fq 'if (flush && curseg->inited)' "$KERNEL/fs/f2fs/segment.c"
          grep -Fq 'if (!get_valid_blocks(sbi, segno, true))' "$KERNEL/fs/f2fs/gc.c"
          ! grep -Rqs 'GC_URGENT_HIGH\|GC_URGENT_LOW\|GC_URGENT_MID\|FI_OPU_WRITE' "$KERNEL/fs/f2fs"
          grep -Fq 'F2FS_MOUNT_AGE_EXTENT_CACHE' "$KERNEL/fs/f2fs/f2fs.h"
          grep -Fq 'struct age_extent_info' "$KERNEL/fs/f2fs/f2fs.h"
          grep -Fq 'f2fs_lookup_age_extent_cache' "$KERNEL/fs/f2fs/extent_cache.c"
          grep -Fq 'age_weighted_average' "$KERNEL/fs/f2fs/extent_cache.c"
          grep -Fq 'atomic64_inc_return(&sbi->allocated_data_blocks)' "$KERNEL/fs/f2fs/segment.c"
          grep -Fq '__get_age_segment_type' "$KERNEL/fs/f2fs/segment.c"
          grep -Fq 'Opt_age_extent_cache' "$KERNEL/fs/f2fs/super.c"
          grep -Fq 'hot_data_age_threshold' "$KERNEL/fs/f2fs/sysfs.c"
          grep -Fq 'warm_data_age_threshold' "$KERNEL/fs/f2fs/sysfs.c"
          awk '/static void default_options/,/^}/' "$KERNEL/fs/f2fs/super.c" | grep -Fq 'set_opt(sbi, AGE_EXTENT_CACHE)'
          grep -Fq 'switch age_extent_cache option is not allowed' "$KERNEL/fs/f2fs/super.c"
          grep -Fq 'P6/d50dfc0c7df7' "$KERNEL/fs/f2fs/checkpoint.c"
          grep -Fq 'P6/e9a844f6e487' "$KERNEL/fs/f2fs/gc.c"
          grep -Fq 'P6/d26fecb03e1f' "$KERNEL/fs/f2fs/segment.h"
          grep -Fq 'P6/c836d3b8d94e' "$KERNEL/fs/f2fs/segment.c"
          grep -Fq 'P6/1005a3ca28e9' "$KERNEL/fs/f2fs/data.c"
          grep -Fq 'P7/238e14eb7226' "$KERNEL/fs/f2fs/node.c"
          grep -Fq 'P7/1773f63d108b' "$KERNEL/fs/f2fs/node.c"
          grep -Fq 'P7/019f9dda7f66+c3e238bd1f56' "$KERNEL/fs/f2fs/node.c"
          grep -Fq 'P7/2d9c4a4ed4ee' "$KERNEL/fs/f2fs/data.c"
          grep -Fq 'P7/ce2739e482bc' "$KERNEL/fs/f2fs/data.c"
          grep -Fq 'P7/05872a167c2c' "$KERNEL/fs/f2fs/f2fs.h"
          # P135: validation-only F2FS sysfs nodes are deliberately removed.
          ! grep -Fq 'atgc_enabled_show' "$KERNEL/fs/f2fs/sysfs.c"
          ! grep -Fq 'age_extent_cache_enabled_show' "$KERNEL/fs/f2fs/sysfs.c"
          ! grep -Fq 'age_extent_tree_count_show' "$KERNEL/fs/f2fs/sysfs.c"
          ! grep -Fq 'age_extent_node_count_show' "$KERNEL/fs/f2fs/sysfs.c"
          ! grep -Fq 'allocated_data_blocks_show' "$KERNEL/fs/f2fs/sysfs.c"
          ! grep -Fq 'ckpt_merge_active_show' "$KERNEL/fs/f2fs/sysfs.c"
          ! grep -Fq 'ckpt_merge_issued_show' "$KERNEL/fs/f2fs/sysfs.c"
          ! grep -Fq 'ckpt_merge_inflight_show' "$KERNEL/fs/f2fs/sysfs.c"
          ! grep -Fq 'ckpt_merge_accumulated_show' "$KERNEL/fs/f2fs/sysfs.c"
          ! grep -Fq 'ckpt_merge_queue_pending_show' "$KERNEL/fs/f2fs/sysfs.c"

          # Internal F2FS feature state stays intact.
          grep -Fq 'atomic64_inc_return(&sbi->allocated_data_blocks)' "$KERNEL/fs/f2fs/segment.c"
          grep -Fq 'f2fs_reinit_atgc_curseg' "$KERNEL/fs/f2fs/checkpoint.c"

          STR="$IMAGE.strings.txt"
          test -s "$STR"
          for token in \
            'a52_sched_diag' \
            'a52_sched_diag_events' \
            'lru_gen_trace' \
            'FUSE_NEGOTIATION_PROBE' \
            'FUSE_740_' \
            'a52_gpu_uv_p1' \
            'a52_gpu_uv_p2' \
            'a52_gpu_uv_p3' \
            'A52 GPU UV P1:' \
            'A52 GPU UV P2:' \
            'A52 GPU UV P3:' \
            'A52 uclamp efficiency P1:' \
            'A52 CASS P3:' \
            'a52_rndis_reftrace' \
            'a52_rndis_fibtrace' \
            'age_extent_cache_enabled' \
            'age_extent_tree_count' \
            'age_extent_node_count' \
            'ckpt_merge_active' \
            'ckpt_merge_issued' \
            'ckpt_merge_inflight' \
            'ckpt_merge_accumulated' \
            'ckpt_merge_queue_pending'
          do
            if grep -Fq "$token" "$STR"; then
              echo "::error::P136 baseline cleanup failed, diagnostic string remains in built image: $token"
              grep -Fn "$token" "$STR" | head -20 || true
              exit 1
            fi
          done

          git -C "$KERNEL" diff --check


      - name: Check out pinned AnyKernel3
        uses: actions/checkout@v4
        with:
          repository: osm0sis/AnyKernel3
          ref: ${{ env.ANYKERNEL_COMMIT }}
          path: packaging/AnyKernel3
          fetch-depth: 1

      - name: Package A619 KGSL P6 + PM P136 flashable ZIP
        run: |
          set -Eeuo pipefail
          mkdir -p release
          cp scripts/09_package_a52xq_anykernel.sh scripts/.package-f2fs325-p1.sh
          sed -i "s/grep -Fxq '# CONFIG_KSU_SUSFS is not set'/grep -Fxq 'CONFIG_KSU_SUSFS=y'/" scripts/.package-f2fs325-p1.sh
          sed -i 's/SUSFS must remain disabled for this package/Kernel config does not enable SUSFS core/' scripts/.package-f2fs325-p1.sh
          python3 - <<'PY'
          from pathlib import Path
          p = Path('scripts/.package-f2fs325-p1.sh')
          s = p.read_text()
          anchor = 'dump_boot;\nwrite_boot;'
          if s.count(anchor) != 1:
              raise SystemExit(f'unexpected boot-write anchor count: {s.count(anchor)}')
          s = s.replace(anchor,
              'dump_boot;\n'
              'patch_cmdline "sched_eevdf" "sched_eevdf=1";\n'
              'patch_cmdline "sched_cass" "sched_cass=1";\n'
              'write_boot;', 1)
          p.write_text(s)
          PY
          python3 scripts/88_patch_anykernel_vulkan_renderengine.py scripts/.package-f2fs325-p1.sh
          sed -i 's/kernel.string=touchGrass A52xq Linux $TARGET_VERSION + ReSukiSU v4.1.0/kernel.string=touchGrass A52xq Linux $TARGET_VERSION + ReSukiSU v4.1.0 + BASE35461116688 + F2FS-STABLE325-P1-GCMERGE-P2-INMEM-P3A-ATGC-P3B-P3C-P4-BLOCKAGE-P5A-P5B-GCCP-P6-CORRECTNESS-P7-BPF-BOOTFIX-P1 + MGLRU-P4 + CASS-P3 + UCLAMP-EFF-P2 + WALT-SUGOV-P2 + GPU-UV-P1-P2-P3 + FUSE-7.40-NATIVE + BPF-DEVMAP-RNDIS-CLEAN-P134 + PROD-NODIAG-P135 + A619-KGSL-P6-PM-P136/' scripts/.package-f2fs325-p1.sh
          sed -i 's/ReSukiSU-v4.1.0-BUILD-VERIFIED-FLASHABLE.zip/A52-A619-KGSL-P6-PM-P136.zip/' scripts/.package-f2fs325-p1.sh
          chmod +x scripts/.package-f2fs325-p1.sh
          ./scripts/.package-f2fs325-p1.sh 4.19.206 "$IMAGE" packaging/AnyKernel3 release
          rm -f scripts/.package-f2fs325-p1.sh

      - name: Verify package
        run: |
          set -Eeuo pipefail
          ZIP="$(find release -maxdepth 1 -name '*.zip' -type f | head -1)"
          test -s "$ZIP"
          unzip -p "$ZIP" anykernel.sh | grep -Fq 'patch_cmdline "sched_eevdf" "sched_eevdf=1";'
          unzip -p "$ZIP" anykernel.sh | grep -Fq 'patch_cmdline "sched_cass" "sched_cass=1";'
          unzip -p "$ZIP" anykernel.sh | grep -Fq 'install_vulkan_re_module;'
          unzip -l "$ZIP" | tee artifacts/package-list.txt
          sha256sum "$ZIP" | tee artifacts/package-sha256.txt

      - name: Save compiler cache
        if: always() && steps.ccache_restore.outputs.cache-hit != 'true'
        uses: actions/cache/save@v4
        with:
          path: .ccache
          key: a52xq-f2fs325-p1-${{ runner.os }}-${{ github.sha }}

      - name: Upload A619 KGSL P6 + PM P136 flashable ZIP
        uses: actions/upload-artifact@v4
        with:
          name: A52XQ-BASE35780015630-A619-KGSL-P6-PM-P136-${{ github.run_number }}
          path: release/
          if-no-files-found: error
          retention-days: 14


# trigger: f2fs-stable325-p1

# trigger: runner-safe-patch-paths

# trigger: executable-build-scripts

# trigger: gc-merge-p2

# trigger: gc-merge-opt-in-safety

# trigger: canonical-gc-merge-bit

# trigger: split-p3a-inmem-curseg-only

# trigger: p3b-atgc-strict-on-booted-p3a

# trigger: samsung-atgc-adapter-build

# trigger: samsung-rbtree-api-fix

# trigger: full-generalized-rbtree-backport

# trigger: atgc-mtime-and-rbtree-prereqs

# trigger: allocator-adaptation-order-fix

# trigger: replace-block-regex-adaptation

# trigger: p3c-atgc-initial-mount-enable

# trigger: p4-newer-atgc-refinements

# trigger: p5a-block-age-dormant

# trigger: p5a-comment-safe-mount-bit-anchor

# trigger: p5b-block-age-activation

# trigger: p6-gc-cp-modernization

# trigger: p7-correctness-integrity

# trigger: bpf-boot-reliability-p1

# diagnostic baseline: actions run 35633887441 / commit 95278a740d6c4ccca32489247f373a0f6ab96982

# diagnostic refinement: P2 FIB object identity + dev_get_by_index parent caller

# fix candidate: upstream ce197d83a9fc devmap_hash unregister cleanup

# packaging fix: keep ZIP filename below filesystem NAME_MAX

# crash repair: restore missing per-entry bulkq + DEVMAP_HASH map-free cleanup

# production phase: no USB RNDIS reftrace/fibtrace instrumentation

# P135: strip all project-added runtime diagnostics, retain production features

# P135 caching: prepared P134 source cache skips repeated F2FS/BPF phase replay


# P136: A619 KGSL modernization + conservative power-management correctness
# baseline workflow run: 35780015630
